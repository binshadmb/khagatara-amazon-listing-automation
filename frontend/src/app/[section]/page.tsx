"use client";
import { useParams } from "next/navigation";
import { AppShell } from "../../components/AppShell";

const copy:Record<string,{title:string;body:string;action:string}>={products:{title:"Products",body:"Manage local product drafts and listing preparation.",action:"New product"},uploads:{title:"Uploads",body:"Upload images, PDFs, spreadsheets, JSON, or product text.",action:"Upload files"},review:{title:"Review Queue",body:"Approve, edit, or reject detected product values.",action:"Open review"},variants:{title:"Variants",body:"Choose dimensions; the backend generates valid combinations and SKUs.",action:"Generate variants"},mapping:{title:"Amazon Mapping",body:"Map confirmed local attributes to the selected Amazon schema.",action:"View mapping"},validation:{title:"Validation",body:"Run required-field, mapping, image, offer, and schema checks.",action:"Validate again"},schemas:{title:"Schemas",body:"Inspect fetched Amazon product schemas. Sandbox placeholders are clearly marked.",action:"Refresh schemas"},attributes:{title:"Attributes",body:"Manage active local categories, attributes, and safely versioned values.",action:"Add attribute"},submissions:{title:"Submission History",body:"Review queued, submitted, accepted, rejected, and retrying listings.",action:"View submissions"},settings:{title:"Settings",body:"Theme, font size, marketplace, upload formats, and developer diagnostics.",action:"Save settings"}};
export default function Section(){const params=useParams<{section:string}>();const page=copy[params.section]??{title:"Not found",body:"This page is not available.",action:"Return to dashboard"};return <AppShell><div className="content"><div className="page-title"><div><h1>{page.title}</h1><p>{page.body}</p></div><button className="primary">{page.action}</button></div><section className="empty panel"><h2>{page.title} workspace</h2><p>This frontend is ready for the Python REST endpoint. The API client is centralized in <code>src/lib/api.ts</code>, so this screen will use backend data as soon as the REST layer is available.</p><div className="placeholder-lines"><i/><i/><i/></div></section></div></AppShell>}
